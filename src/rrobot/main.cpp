

/**
 *  Reads Json, creates environmentProcessor then pushes this to mapper.
 * 
 * Mapper makes the actual call to environmentProcessor, so memory is cleaned up in this method.
 * 
 * mappers init mehtod 
 */
int setUp(int& mapper) {
    return mapper;
}